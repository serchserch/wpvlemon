{% extends "QPvBundle::template.html.twig" %}

{% block content %}
{% include 'QPvBundle::datepicker.html.twig'%}

    <h2>Edita tu información</h2>

    
    
    <form action="{{ path('u_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {{ form_widget(edit_form, {'attr' : {'class' : 'form_new'}}) }}
        <p>
            <button class="fsbutton_ok" type="submit">Editar</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a class="fsbutton_cancel" href="{{ path('panel_index') }}">
                Cancelar
            </a>
        </li>
    </ul>
    
{% if m == 1%}
{% include 'QPvBundle::fancybox.html.twig'%}
<script type="text/javascript">
$(window).load(function() {
    $('#trigger').trigger('click');
});
</script>
<a id="trigger" style="display: none;" class="fancybox" href="#datos_modificados" title="Tus datos se han modificado" >Punto de Venta</a>
<div id="datos_modificados" style="width:300px;display: none;">
    <p>Tus datos se han modificado</p>
</div>
{% endif %}
{% endblock %}
