{% extends "QPvBundle::template.html.twig" %}
{% block title %}Almacenes - Editar{% endblock %}
{% block content %}
    <h1>Editar</h1>

    <form action="{{ path('panel_almacen_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        <input type="hidden" name="_method" value="PUT" />
        {% form_theme edit_form 'QPvBundle:Error:error_field.html.twig'%}
        {{ form_widget(edit_form, {'attr' : {'class' : 'form_new'}}) }}
        <p>
            <button class="fsbutton_ok" type="submit">Editar</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a class="fsbutton_cancel" href="{{ path('almacen_index') }}">
            Cancelar
        </a>
    </li>
    <li>
        {#
        <form action="{{ path('panel_almacen_delete', { 'id': entity.id }) }}" method="post">
            <input type="hidden" name="_method" value="DELETE" />
            {{ form_widget(delete_form) }}
            <button type="submit">Borrar</button>
        </form>
        #}
    </li>
</ul>
{% endblock %}
